# Plan: Chapter 3 — Remove hyperparameters table

## Changes required

### 1. Remove the table block (lines 201–234)
Delete the entire `\begin{table}...\end{table}` block for `tab:hyperparameters`.

### 2. Remove the reference sentence (line 199)
Delete: "Bảng~\ref{tab:hyperparameters} tổng hợp toàn bộ các siêu tham số của PhaBERT-CNN trong cả hai giai đoạn huấn luyện."

### 3. Keep everything else unchanged
- Section intro (line 178) — no change
- Subsection Giai Đoạn 1 (lines 180–182) — no change
- Subsection Giai Đoạn 2 (lines 184–186) — no change
- Subsection Hàm Mất Mát (lines 188–197) — no change
- Hardware sentence (line 236) — no change

## No new citations, no new numbers.